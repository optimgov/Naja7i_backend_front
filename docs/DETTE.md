# Fichier de dette technique

| ID | Constat | Origine | Échéance |
|---|---|---|---|
| ~~DET-01~~ | ~~`Role` n'a pas d'UUID public. Trancher : `Role::code` est-il une exception publique documentée à la règle UUIDv7 ?~~ **Clos au PAS-9** : la question est tranchée dans le sens de la règle, pas de l'exception. `Role` reçoit `HasPublicUuid` et la colonne `uuid` (NOT NULL, unique), les sept rôles de plateforme étant complétés par la migration. `code` reste lisible mais cesse d'être l'identifiant de référence — il ne pouvait plus l'être, n'étant plus unique globalement depuis que les organismes définissent leurs propres rôles. | Revue PAS-1, R6 | clos · PAS-9 |
| DET-02 | Une requête pour résoudre le tenant plateforme à chaque requête HTTP. Ne pas corriger par un cache statique (ce serait recréer BLOC-2). | Revue PAS-1, P7 | Quand la latence le justifie |
| DET-03 | `users_email_or_phone` ne garantit pas une méthode de connexion utilisable. Invariant applicatif à poser avec les identités sociales. | Revue PAS-1, §4.4 | PAS-3 |
| DET-04 | Anonymisation CNDP vs `cascadeOnDelete` sur `memberships.user_id`. Politique de rétention à écrire. | Revue PAS-1, §4.5 | Lot conformité |
| DET-05 | Aucune vérification n'empêche une future migration de violer la matrice « catalogue global / activité isolée ». | Revue PAS-1, R3 | PAS-5 |
| DET-06 | Normalisation des e-mails et téléphones : chaînes vides encore possibles selon le chemin d'écriture. | Revue PAS-1, §4.4 | PAS-3 |
| DET-07 | **Textes juridiques provisoires.** `legal_documents` contient des placeholders marqués `0.1-provisoire`. La mise en ligne publique est bloquée tant que les textes FR et AR validés par un conseil juridique marocain ne sont pas fournis, avec version et empreinte. | PAS-2, ADR-0005 | **Avant ouverture publique** |
| DET-08 | Qualification juridique des trois actes (contrat / information / consentement) décidée en architecture, non validée par un juriste. | PAS-2, ADR-0005 | Avant ouverture publique |
| DET-09 | Fournisseur d'e-mail non choisi : domaine d'expédition, SPF, DKIM, DMARC, gestion des rebonds. Ne bloque pas le développement, bloque les tests de délivrabilité. | PAS-2, Q4 | Avant pilote |
| DET-10 | Écart NIST assumé (12 caractères au lieu de 15) sans second facteur. À rouvrir quand le MFA existera. | PAS-2, ADR-0007 | Lot sécurité |
| ~~DET-11~~ | ~~Signature des liens de vérification derrière le proxy Nitro.~~ **Clos au PAS-3** : les URL signées sont abandonnées au profit de jetons opaques, précisément parce que la signature ne survit pas au relais (ADR-0008 §4). Le risque n'est plus à tester, il n'existe plus. | PAS-2, D2-12 | clos · PAS-3 |
| ~~DET-12~~ | ~~Le lien de vérification pointe vers l'API et répond du JSON.~~ **Clos au PAS-3** : le lien mène à `/{locale}/verifier-email` sur le frontend, qui poste le jeton à l'API. Vérifié dans Mailpit en FR et en AR. | PAS-2 | clos · PAS-3 |
| DET-13 | `RegisterRequest` s'arrête à la première règle en défaut : le formulaire d'inscription ne peut donc pas signaler plusieurs champs d'un coup. Acceptable sur cinq champs ; à rouvrir si le profil progressif (PAS-4) allonge le formulaire. | PAS-2 | PAS-4 |
| DET-14 | **Envoi synchrone.** Les notifications n'implémentent pas `ShouldQueue` : l'aller-retour SMTP se fait pendant la requête d'inscription. Un fournisseur lent ralentit l'inscription ; un fournisseur en panne la fait échouer en 500 alors que le compte est déjà créé. À basculer sur la file (Redis est en place) en même temps que le choix du fournisseur. | PAS-3 | Avec DET-09 |
| DET-15 | Les endpoints publics `resend` et `password/request` sont limités par adresse e-mail, pas par IP : un appelant qui fait tourner les adresses n'est borné que par `throttle:6,1`. Suffisant tant que l'envoi est peu coûteux ; à revoir quand le fournisseur facturera au message. | PAS-3, ADR-0008 §5 | Avec DET-09 |
| DET-16 | **Les huit codes de cause ne sont pas validés pédagogiquement.** Proposition d'architecte, non confirmée par un responsable pédagogique CRMEF : couverture des erreurs réellement observées, absence de redondance. Modifier la liste après le début de l'étiquetage impose de réétiqueter les questions existantes — le coût croît avec le volume produit. | F03 v1.1, réserve pédagogique | **Avant le premier étiquetage en volume (PAS-6)** |
| DET-17 | Les formulations candidat de F03 (FR et AR) sont des propositions non finalisées : contrainte de ton « hypothèse et non verdict, aucun jugement sur la personne » posée par la fiche, mais textes définitifs à arrêter avec un responsable pédagogique. Un test d'acceptation de F03 porte sur cette contrainte, il ne peut pas être écrit contre un texte provisoire. | F03 v1.1, « Ce que voit le candidat » | Avant ouverture publique |
| ~~DET-18~~ | ~~`TenancyArchitectureTest` étend `PHPUnit\Framework\TestCase` tout en appelant `base_path()` : il ne passe que par effet de bord de l'ordre d'exécution. Lancé seul, parallélisé ou réordonné, il virerait au vert **sans rien vérifier** — une garde de sécurité qui cesse de fonctionner en silence.~~ **Corrigé au PAS-7, étape 3** : `extends Tests\TestCase`. Vérifié par sonde en exécution isolée — la garde passe seule, et détecte seule un accès brut à `attempts` comme à `mastery_scores`. | Revue PAS-6 par Claude Code | clos · PAS-7 |
| DET-19 | Le coefficient 0,35 attribué à une réussite « au hasard » est une valeur d'architecte. À réétalonner sur données réelles dès que le volume de réponses le permet. | ADR-0017 §2 | Après 1 000 réponses réelles |
| DET-20 | Le recalcul de maîtrise est synchrone à la soumission. Acceptable au volume actuel ; à basculer en abonné de l'événement `AttemptSubmitted` avant la montée en charge. | ADR-0017 | Avant 500 candidats simultanés |
| DET-21 | Annotation CI : `actions/checkout@v4` sur Node 20, déprécié. Signalé depuis le PAS-5. | CI GitHub | Prochain lot d'outillage |
| DET-22 | `RemediationPlanner::FACTEUR_JAMAIS_EVALUE` (0,5) : un domaine jamais évalué entre dans l'ordonnance à moitié d'écart. Valeur d'architecte, de même nature que le 0,35 de DET-19 — elle arbitre entre « faire découvrir » et « faire corriger », arbitrage qui ne se tranche que sur données réelles. Le code livré au PAS-7 comptait un domaine non testé comme un échec total, ce qui faisait passer l'inconnu avant la faiblesse démontrée. | Revue PAS-7 par Claude Code, ADR-0017 §5 | Avec DET-19 |
| DET-23 | Modèle d'accès organisme non défini : par siège acheté, ou illimité pour les membres ? Détermine toute la facturation B2B. | ADR-0013 | Avant le premier contrat organisme |
| DET-24 | Sort de la progression d'un membre qui quitte l'organisme. Position retenue : elle lui appartient et le suit. À confirmer par écrit dans les conventions. | ADR-0013 | Avant le premier contrat organisme |
| DET-25 | Un organisme voit-il les résultats individuels de ses membres ? Question de protection des données, pas de fonctionnalité. Position par défaut : agrégats uniquement, jamais le détail nominatif. | ADR-0013 | Avant le premier contrat organisme |
| DET-26 | Un organisme peut-il restreindre les concours accessibles à ses membres ? Change la conception du catalogue vu par le candidat. | ADR-0013 | Avant le premier contrat organisme |
| DET-27 | Le niveau « domaine » du CRMEF n'est défini dans aucun document. Doit être écrit par les responsables pédagogiques avant la production de contenu. Numéroté DET-16 dans l'annexe organisme jamais fusionnée, d'où les renvois de `CatalogueSeeder` corrigés en même temps que cette fusion. | ADR-0012 | Avant la production de contenu |
| DET-28 | **La suite ne peut pas être parallélisée, et la raison n'est pas celle qui était consignée.** `php artisan test --parallel` n'atteint jamais l'exécution : Collision 8.x exige `brianium/paratest` ^7, absent de `composer.json` (il n'apparaît dans `composer.lock` qu'en suggestion d'un autre paquet). L'interblocage sur `DROP TABLE` décrit au BACKLOG §6 et §9 est donc un constat **hérité, jamais reproduit** en l'état du dépôt. Le vérifier suppose d'ajouter paratest en dépendance de développement — une décision de dépendance, pas un constat, volontairement laissée hors de ce lot. En attendant, la suite tourne en séquentiel (~75 s en local, ~1 min 50 en CI), ce qui reste confortable. **REQUALIFIÉ AU PAS-20 : le problème n'était pas le séquentiel.** Mesure avant d'agir, et elle a inversé la décision. Profil par classe : la répartition est PLATE (six classes sur vingt-six portent la moitié du temps), donc aucun point chaud à traiter — mais **67 % de la suite était du montage**, pas des assertions. Deux causes, aucune dépendance : (1) onze classes rejouaient `CatalogueSeeder` et `Crmef2025Seeder` dans leur `setUp()`, donc à chaque test et à l'intérieur d'une transaction annulée ensuite — 0,22 s par test, ~46 s ; (2) `HASH_DRIVER=argon2id` en test au coût de PRODUCTION, mesuré à 99,9 ms par hachage quand chaque test crée deux à trois comptes — ~92 s, le premier poste de la suite, loin devant les semis. Résultat : **249 s → ~117 s**, trois exécutions vertes dont une en ordre aléatoire. Paratest N'A PAS été installé, et ne le sera pas tant que la durée ne le redemandera pas : ajouter une dépendance et une classe de défauts pour paralléliser un montage devenu court serait un mauvais échange. **Conséquence à ne pas perdre : l'interblocage sur `DROP TABLE` reste non reproduit ET non vérifié** — le vérifier exige paratest. Il n'est ni infirmé ni confirmé. | Revue PAS-9 par Claude Code, étape 6 ; mesuré au PAS-20 | Si la durée redevient un problème |
| DET-39 | **Le catalogue de test vit désormais HORS de la transaction de test.** Depuis le PAS-20, `Tests\TestCase::$seed` fait poser `CatalogueSeeder` et `Crmef2025Seeder` par `migrate:fresh --seed`, une seule fois par processus et avant l'ouverture de la transaction. Tout ce qu'un test crée reste annulé ; en revanche, un test qui MODIFIERAIT une ligne de catalogue hors transaction — par une commande, un `DB::unprepared`, ou un futur `RefreshDatabase` contourné — verrait sa modification survivre aux tests suivants. Aucun test ne le fait aujourd'hui, et une exécution en ordre aléatoire le confirme. C'est une contrainte de contribution, pas un défaut : elle est écrite ici parce qu'elle ne se déduit d'aucune ligne de code. | PAS-20, `Tests\TestCase` | Surveillance — à rouvrir au premier test qui écrit dans le catalogue |
| DET-29 | `memberships_role_scope_guard` ne couvre pas `DELETE` — vérifié en base : l'événement est `INSERT UPDATE`. Une suppression d'appartenance ne prend donc pas le rendez-vous sur la ligne `roles`, et le verrou sur `memberships` posé au PAS-14 fait alors ATTENDRE la garde des rôles au lieu de la laisser refuser par prudence sur un décompte encore élevé — l'inverse de l'effet recherché. Ce n'est pas une faille : retirer une appartenance ne peut que réduire les privilèges, et l'issue reste correcte dans les deux cas. Tracé parce que l'asymétrie n'est écrite nulle part et se paiera au premier chemin qui supprimera des appartenances en volume. | Revue PAS-14 par OptimGov et Claude Code | Avec le premier lot qui supprime des appartenances |
| DET-30 | `Exam` n'emploie pas le trait `IsCatalogueEntry`, contrairement aux autres entrées de catalogue : il porte son propre `scopePublished` et n'a aucun `scopeOrdered`. Tout appelant qui veut des épreuves ordonnées doit donc réécrire `orderBy('position')->orderBy('name_fr')` à la main — c'est déjà le cas dans `CatalogueController::family()`. Rien ne casse aujourd'hui, mais la convention est rompue en silence et le second appelant divergera du premier sans que rien ne le signale. Deux issues : faire employer le trait à `Exam`, ou lui donner un `scopeOrdered` explicite. | Application du FRONT-2, revue par Claude Code | Au prochain lot qui liste des épreuves |
| DET-31 | `attempts.correct_count` et `AttemptResource.correct_count` ne distinguent pas le `kind` : un 9/10 d'entraînement se sérialise exactement comme un 9/10 de diagnostic. Or une série d'entraînement est ciblée sur un domaine faible et n'est pas représentative du concours — par construction. Aucun écran ne les confond aujourd'hui, mais rien n'empêche une interface d'afficher « 90 % » à un candidat qui vient de réviser son point faible, et de lui faire croire qu'il est prêt. Le champ existe depuis le PAS-6 et n'est pas modifié ici : signalé plutôt que corrigé, comme demandé. | PAS-15, revue par Claude Code | Avec le premier écran de restitution d'entraînement |
| DET-32 | **Les constantes de `MemoryScheduler` sont des valeurs d'architecte.** Les paliers `1-3-7-16-35`, `SORTIES_CONSECUTIVES` (2), `MARGE_AVANT_EPREUVE` (2 jours), `PLAFOND_LISTE` (20 rendez-vous par séance depuis le PAS-18) et `PLAFOND_ENONCE_RESSERVI` (palier 3, soit 7 jours, depuis le PAS-22) fixent respectivement le rythme du calendrier, le moment où une compétence en sort, la date au-delà de laquelle un rendez-vous n'aide plus, et la taille d'une journée de révision. Aucune n'est mesurée : elles ont été choisies pour être explicables au candidat (« revu dans 3 jours ») plutôt que pour être justes, ce qui était le bon arbitrage sur une banque jeune sans historique de calibration, et reste un arbitrage. Même nature que le 0,35 de DET-19 et le 0,5 de DET-22. Le coût du réétalonnage croît avec le nombre de rendez-vous déjà planifiés : changer un palier ne réécrit pas les lignes existantes, la cohorte se retrouve donc à cheval sur deux rythmes. | PAS-16 (F07), `MemoryScheduler` | Avec DET-19 |
| DET-33 | **`naja7i.timezone_candidat` est une clé globale, elle deviendra une colonne.** Le fuseau du candidat décide de la date d'un rendez-vous : le projet n'en avait aucun, `app.timezone` est en UTC, et F07 a posé une clé unique sur `Africa/Casablanca` — l'audience est marocaine et une préférence par compte serait une complexité sans demandeur. La bascule est prévisible : au premier candidat de la diaspora, la clé devient une colonne d'utilisateur. Le module Opportunités doit consommer cette clé plutôt que d'en déclarer une seconde, sans quoi deux fuseaux coexisteront. | PAS-16 (F07), `config/naja7i.php` | Au premier candidat hors fuseau marocain |
| ~~DET-34~~ | ~~**La plateforme récompense l'évitement.** `MasteryCalculator` part de `responses` et joint vers `attempt_items` : un item sans réponse n'a pas de ligne, il n'existe donc pas pour le calcul. Deux candidats sur la même série de dix, cinq bonnes réponses chacun — celui qui répond faux aux cinq autres tombe à 50 et prend la tête de l'ordonnance, celui qui les saute affiche 100 et en sort. Le domaine faible du second disparaît précisément parce qu'il l'a esquivé.~~ **Clos au PAS-17**, le pas où il est inscrit : le constat n'a pas attendu son tour. `mastery_scores.skipped_count` compte les questions servies sur une tentative close et laissées sans réponse, et `RemediationPlanner` les fait entrer dans l'urgence à facteur partiel, avec le motif distinct `questions_sautees`. Le score, lui, n'est pas touché : les sautées n'entrent pas au dénominateur (voir ADR-0017 et le commentaire de `MasteryCalculator::ecrire`). | Note de reprise F07, mesuré | clos · PAS-17 |
| ~~DET-35~~ | ~~**Le calendrier n'avance que si la question TRACÉE est resservie.** Un rendez-vous porte le couple (compétence, cause) mais retrouve sa réussite par `last_question_id`, l'appariement n'étant pas symétrique — une réponse fausse porte la cause du distracteur choisi, une réponse juste n'en porte aucune. Conséquence : une session d'entraînement ne fait progresser aucun palier, sauf coïncidence sur la question exacte. Un candidat qui travaille beaucoup verra donc revenir les mêmes rendez-vous, ce qui est l'inverse de l'effet recherché. La question à trancher est nette : tout passage sur le couple (compétence, cause) doit-il faire avancer le palier, ou seulement la question tracée ?~~ **Tranché et clos au PAS-18, dans le sens du COUPLE.** Un rendez-vous avance dès que le candidat rencontre le couple (compétence, cause) et n'y tombe pas, quel que soit le véhicule — révision, entraînement ou diagnostic. La lecture d'une réussite passe par les distracteurs : une réponse juste à une question dont un distracteur porte la cause X, dans la même compétence, prouve que le piège X n'a pas fonctionné. `last_question_id` change de rôle et ne sert plus qu'à l'anti-répétition. Deux garde-fous nés de la mesure accompagnent l'arbitrage — un couple ne bouge qu'UNE fois par tentative, et l'échec du jour l'emporte sur les réussites du même jour ; sans eux, une séance de six questions créait un rendez-vous puis le refermait elle-même. | Note de reprise F07 | clos · PAS-18 |
| ~~DET-36~~ | ~~**Les effets de bord de la soumission vivent dans le contrôleur, pas dans le service.** `ParcoursController::submit()` appelle `AttemptService::submit()`, puis `MasteryCalculator::recomputeForExam()`, puis `MemoryScheduler::planFromAttempt()`. Une soumission par un autre chemin — commande de clôture des tentatives expirées, back-office, futur abonné d'événement — ne recalculerait donc pas la maîtrise et ne planifierait aucun rendez-vous, en silence et sans rien casser d'observable. L'endroit juste est `AttemptService::submit()`, où `is_correct` vient d'être figé. À traiter avec DET-20, qui déplacera de toute façon le recalcul vers un abonné de `AttemptSubmitted` : les deux corrections touchent le même point. **Vérifié au PAS-18** : l'accroche voit bien TOUTES les tentatives closes, pas seulement les révisions — un test le tient, l'arbitrage DET-35 en dépendant entièrement. Le défaut reste donc de placement, pas de couverture, et la dette reste ouverte pour cette seule raison.~~ **Clos au PAS-21, et ce n'était pas qu'un défaut de placement.** L'audit externe a montré ce que le mauvais endroit coûtait : `submit()` rend sans bruit une tentative déjà close, et le contrôleur appelait les effets SANS CONDITION — un client qui soumet, perd la réponse HTTP et rejoue le même POST faisait avancer `consecutive_sure` deux fois, et deux réussites certaines suffisant à sortir, un simple renvoi réseau pouvait vider un rendez-vous. Recalcul de maîtrise et planification vivent désormais dans `AttemptService::submit()`, DANS la transaction et DERRIÈRE la garde de transition ; toute autre voie de soumission en bénéficie. Coût mesuré : 41 ms et 42 requêtes pour une série de dix dont cinq erreurs. | Note de reprise F07 ; audit externe 490fc53 BLOC-1 | clos · PAS-21 |
| DET-40 | **Les horodatages sont à la SECONDE : aucun test d'idempotence ne peut s'y fier.** Les colonnes `timestampTz` du schéma n'ont pas de précision fractionnaire — `updated_at`, `last_reviewed_at`, `created_at` se sérialisent en `.000000`. Deux appels HTTP consécutifs tombent dans la même seconde et produisent des horodatages IDENTIQUES. Découvert en vérifiant par mutation le test du BLOC-1 : il comparait toutes les colonnes du rendez-vous avant et après un rejeu, restait vert en retirant la garde, et ne prouvait donc rien. Le test a été refait pour porter sur un compteur qui incrémente. **Toute assertion « rien n'a bougé » fondée sur un horodatage est silencieusement faible dans ce dépôt** — et il n'existe aucun garde-fou qui le signale. Deux issues : donner une précision aux colonnes, ou poser la règle par écrit et l'appliquer en revue. | PAS-21, vérification par mutation | Au prochain test qui voudra prouver une non-écriture |
| ~~DET-43~~ | ~~**`AuthenticationTest` domine la suite et son temps varie de ±70 % à code identique.** Mesuré au PAS-24, trois exécutions consécutives du MÊME code : 86 s, 51 s, 85 s. La classe pèse à elle seule 40 à 60 % du temps total, et ses tests lents sont tous ceux qui inscrivent ou connectent (3 à 9 s chacun). Le hachage est hors de cause — vérifié à 0,9 ms sous les réglages de test, les variables d'argon2id du PAS-20 s'appliquant bien. La cause n'est pas identifiée. **Conséquence à ne pas perdre : toute mesure du temps de la suite est bruitée par cette classe, y compris celles du PAS-20.** Comparer deux révisions sur une seule exécution ne prouve rien — il faut une médiane sur trois, et le dire. À instrumenter avant tout nouveau travail sur la durée de la suite, faute de quoi on optimisera au hasard.~~ **Clos au PAS-25 en « environnement », sur mesure.** Trois exécutions sous `/usr/bin/time` : la classe tient en **6,3 à 7,4 s** d'horloge pour **1,2 s de CPU**, et le rapport JUnit donne 7,20 / 6,55 / 6,26 s — variance ±7 %, ÉTALÉE sur les tests, aucun point chaud. Le fait décisif : **le CPU reste à ~1,2 s dans TOUTES les exécutions, de 6 s à 86 s d'horloge**. Aucun travail supplémentaire n'est jamais fait ; les excursions sont de l'attente pure. Trois causes écartées par la mesure — le lanceur (`phpunit` et `artisan test` s'interclassent dans les deux sens sur quatre exécutions dos à dos), l'environnement (`ARGON_MEMORY`, `ARGON_TIME`, `HASH_DRIVER` et `DB_DATABASE` identiques sous les deux, hachage à 2,4 ms), et la base (aller-retour à 0,6 ms, stable sur 1 500 requêtes). Charge de l'hôte : 3,7 à 4,1. **Conséquence de méthode, elle, à garder : une mesure de durée est une MÉDIANE SUR TROIS et se dit comme telle** — les chiffres ponctuels du PAS-20 et du PAS-24 étaient bruités par ce même hôte. La décision d'écarter paratest reste bonne, son gain mesuré (facteur deux) dépassant largement ce bruit. | PAS-24, mesuré | clos · PAS-25 |
| ~~DET-42~~ | ~~**L'épreuve suivie se DÉDUIT de la tentative la plus récente, elle n'est pas déclarée.** `GET me/attempts` permet au frontend de retrouver l'épreuve d'un candidat sans profil ni trace locale — aucun état nouveau, tout se déduit de faits existants, et c'est ce qui rend cette route peu coûteuse. Mais la déduction est fausse dans un cas réel : un candidat qui prépare l'épreuve A et a travaillé B en dernier — par curiosité, par un entraînement d'essai — verra B lui être présentée comme son épreuve. Le produit n'a aujourd'hui aucun moyen de savoir laquelle il PRÉPARE, seulement laquelle il a TOUCHÉE. Le profil candidat (lot non ouvert) porte cette déclaration et remplacera la déduction ; jusque-là, un frontend qui bâtit un écran d'accueil sur cette base doit savoir qu'il devine.~~ **Clos au pas `64df5af` : l'épreuve préparée se DÉCLARE.** `GET/PUT me/profile` portent `exam_code`, l'objectif et l'échéance visée ; l'épreuve doit être publiée, et le refus emprunte le message volontairement ambigu du PAS-4 plutôt que de laisser deviner le catalogue à venir. **Le profil REMPLACE la déduction, il ne s'ajoute pas à côté** — aucune déduction ne subsiste côté serveur, et le contrat écrit au backlog dit ce que le frontend doit en faire : quand `exam_code` est nul, PROPOSER la dernière épreuve travaillée comme suggestion à confirmer, jamais l'appliquer. Un profil absent rend les mêmes clés à `null`, pas une 404 : un compte neuf n'a rien choisi, et répondre « introuvable » à qui demande son propre profil serait un mensonge doublé d'un piège pour l'appelant. | PAS-23 | clos · `64df5af` |
| ~~DET-46~~ | ~~**AUCUN chemin applicatif ne pose `question_sources.verification = 'verified'`, et la chaîne éditoriale bute donc sur son propre dernier verrou.** Publier pour diagnostic ou simulation exige une source VÉRIFIÉE — `QuestionIntegrityChecker` l'exige, un trigger l'impose en base. Or la colonne naît `unverified` et rien, dans aucun service ni aucune route, ne la fait passer à `verified` : seuls les montages de test l'écrivent en attachant directement. **Conséquence : un rédacteur peut écrire, faire relire, faire valider — et la publication pour diagnostic sera refusée, sans qu'aucune surface ne permette de lever le blocage.** Le PAS-27 n'a rien inventé pour combler ce trou, comme la consigne le demandait : citer une source n'est pas la vérifier, et laisser un rédacteur certifier la sienne annulerait la règle. Il manque une DÉCISION, pas du code : qui vérifie une source, sous quelle permission, et à quel moment de la chaîne. `questions.review` est le candidat naturel, mais rien ne l'écrit.~~ **Tranché et clos au PAS-28 : vérifier est un acte sur LA SOURCE, pas sur la question.** Une source est citée par plusieurs questions ; la vérifier une fois profite à toutes. En faire un acte par question ferait recontrôler vingt fois le même arrêté ministériel sans que rien ne garantisse que les vingt verdicts concordent. D'où `POST admin/sources/{uuid}/verify`, qui enregistre QUI et QUAND — sans traçabilité, `verified` est une case cochée qui n'engage personne, et une contrainte de base refuse l'un sans l'autre. Permission `questions.review`, **par commodité et non par principe** : le relecteur a la source sous les yeux, et créer un rôle de documentaliste pour une équipe qui n'en a pas serait de la cérémonie ; le jour où quelqu'un vérifie des sources sans relire de questions, ce devient `sources.verify`. | PAS-27, mesuré | clos · PAS-28 |
| ~~DET-48~~ | ~~**Le miroir d'une question publiée ne peut plus être désigné.** `mirror_question_id` tombe sous le gel du contenu publié (ADR-0015 §5, liste blanche du PAS-11) : un rédacteur qui écrit une bonne sœur APRÈS la publication ne peut plus la désigner, il doit passer par une nouvelle version — donc retirer la question servie et en republier une. Constaté au PAS-30 en livrant DET-45, quand le trigger a refusé le test. **La question est de savoir si ce pointeur est du CONTENU.** Il ne change rien à ce que le candidat lit de cette question-là : il désigne la question qui la vérifie, comme `eligible_for_diagnostic` désigne l'usage — et celui-ci est explicitement hors gel. L'y faire entrer serait cohérent ; l'y laisser aussi, si l'on tient qu'un miroir fait partie de ce qui a été relu. **AVIS D'OptimGov, PAS-31 : le pointeur est de l'USAGE, pas du contenu.** Désigner un autre miroir ne change rien à ce qu'un candidat a VU — `mirror_available` se calcule à la lecture, et aucune correction déjà servie n'y fait référence. Le précédent existe : `eligible_for_diagnostic` échappe au gel pour exactement cette raison. **Le dégel sera un pas court et délibéré, pas un effet de bord d'A4** ; d'ici là, le champ en lecture seule sur une question publiée est le bon comportement, et l'interface DIT pourquoi il est verrouillé plutôt que de le griser sans explication.~~ **Clos au pas `49f8b4b`, dans le sens de l'arbitrage.** `mirror_question_id` rejoint la soustraction `to_jsonb(OLD) - '…'` d'`assert_published_question_frozen` — le mécanisme même qui exempte `eligible_for_diagnostic`, et aucun autre : une exemption écrite ailleurs serait une seconde règle de gel. **Ce qui encadrait la désignation reste entier** — `questions_mirror_not_self`, la clé étrangère, et l'exigence de langue et de statut servable que `QuestionsSoeurs::designee()` applique À LA LECTURE avec repli sur le couple. La question RETIRÉE reste un mur complet : elle n'est plus servie. **L'acte est distinct de l'amendement** — `QuestionAuthoringService::designerMiroir()`, sous `questions.publish`, servi par une action de la liste et non par le formulaire, que `QuestionPolicy::update()` ferme parce que tout le reste y est gelé. **Un test du PAS-10 a quitté sa liste de colonnes gelées, et ce n'est pas lui qui était faux :** c'est la règle qui a changé. | PAS-30, constaté | clos · `49f8b4b` |
| DET-47 | **Une source vérifiée peut être modifiée après coup, et rien ne l'en empêche.** Le gel du PAS-12 couvre les CITATIONS d'une question publiée (`question_sources`) et le contenu de la question, pas la ligne `sources` elle-même. Rien n'interdit donc de changer le titre, l'autorité ou l'URL d'une source après son contrôle : `verified_at` et `verified_by` restent en place et attestent d'un document qui n'est plus celui qui a été vu. Constaté au PAS-28 en traitant DET-46, **signalé et non corrigé comme demandé**. Trois issues, à peser ensemble : geler une source vérifiée comme on gèle une question publiée ; ou invalider le contrôle à toute modification ; ou versionner la source comme on versionne une question. La troisième est la plus cohérente avec ce dépôt, et la plus coûteuse. **MESURE D'ATTENTE POSÉE AU PAS-29, ET LA DETTE RESTE OUVERTE.** Deux déclencheurs : modifier une colonne porteuse de sens — `code`, `kind`, `title_*`, `authority_*`, `session_label`, `url` — annule `verified_at` et `verified_by`, et rétrograde les citations des questions non gelées. La publication pour diagnostic se rebloque donc jusqu'à re-vérification : ça échoue du bon côté, là où la ligne attestait auparavant d'un document qui n'existait plus. `languages` et `location_note_*` en sont exclus — ils aident à trouver le document sans le constituer, et un garde-fou qui crie pour rien finit désarmé. **Ce n'est pas la solution :** l'invalidation efface une trace au lieu de la conserver, et une source corrigée d'une virgule impose un recontrôle complet. Le VERSIONNEMENT — une source vérifiée devient immuable, la corriger en crée une version — reste la réponse, et c'est pour lui que cette ligne demeure. | PAS-28, constaté | Avant la première publication qui s'appuie sur une source vérifiée en volume |
| ~~DET-45~~ | ~~**`questions.mirror_question_id` existe depuis le PAS-5 et F05 ne s'en sert pas.** La colonne — avec sa contrainte `mirror_question_id <> id` et son gel après publication — permet à un RÉDACTEUR de désigner explicitement la question miroir d'un item. Le PAS-26 choisit le miroir autrement : n'importe quelle question de la même compétence dont un distracteur porte la même cause, par `QuestionsSoeurs`. Les deux se défendent, et ce n'est pas au correctif de trancher : un miroir désigné à la main est plus délibéré qu'un miroir déduit, mais il suppose un travail éditorial que le back-office (lot A4) ne permet pas encore, et il serait nul sur presque toute la banque. **Le risque est qu'elle devienne un champ mort dont personne ne sait plus s'il fait autorité.** Deux issues : lui donner la PRIORITÉ quand elle est renseignée et garder le couple en repli, ou la retirer.~~ **Tranché et clos au PAS-30, dans le sens de la priorité.** Le sélecteur de F05 sert la question désignée quand elle l'est, et se replie sur le couple (compétence, cause) sinon : un miroir choisi à la main est plus délibéré qu'un miroir déduit, et le repli couvre le reste de la banque, où rien n'est désigné. **Une désignation non SERVABLE — brouillon, retirée, autre langue — se replie elle aussi** : la désignation dit « c'est cette question-là », elle ne peut pas dire « sers-la même si elle n'a pas passé la relecture ». **Contrainte découverte en livrant, à connaître pour le lot A4 :** `mirror_question_id` est GELÉ après publication comme le reste du contenu (ADR-0015 §5). Le miroir se désigne donc à la rédaction, ou par une nouvelle version — voir DET-48. | PAS-26 | clos · PAS-30 |
| DET-44 | **`AGENTS.md` est un lien symbolique : un clone Windows sans `core.symlinks` recevra un fichier de 9 octets au lieu des instructions.** Assumé — aucun contributeur n'est sous Windows, et `git config core.symlinks true` suffit le jour venu. Inscrit pour que la question ne se repose pas. | PAS-25, `976e0bf` | Au premier contributeur sous Windows |
| ~~DET-41~~ | ~~**Le plan de rédaction n'est lisible que par l'API.** `GET admin/banque/couverture/{examCode}` expose les couples (compétence, cause) que la banque ne couvre pas, ordonnés par nombre de candidats en attente — c'est le meilleur plan de rédaction dont dispose le produit, établi par l'usage. Il n'a aujourd'hui aucune interface : le back-office Filament n'est pas ouvert (lot A4), et un rédacteur ne lit pas du JSON. La donnée existe et se périme donc en silence. Le back-office devra CONSOMMER `CouvertureBanque` plutôt que refaire la requête — un second calcul divergerait du premier au premier changement de `Question::scopeForDiagnostic`, que la sous-requête corrélée recopie déjà.~~ **Clos au lot A4 : la page de couverture est l'ACCUEIL du panneau, et elle consomme le service.** Un back-office qui ouvre sur une liste de questions demande au rédacteur de choisir sur quoi travailler ; celui-ci le lui dit, ordonné par candidats en attente. `App\Filament\Pages\Couverture` appelle `CouvertureBanque::trous()` et n'ajoute aucun calcul — ni seuil, ni sévérité, ni tri : la mutation qui réordonne la collection APRÈS le service fait rougir le test d'ordre, ce qui prouve que la page ne fait qu'afficher. La distinction par langue est rendue en deux colonnes plutôt qu'en une sévérité unique : « deux sœurs en français, aucune en arabe » désigne deux travaux de rédaction distincts, dont un seul est urgent. | PAS-22 | clos · lot A4 |
| DET-37 | **Une seule bonne réponse peut faire avancer jusqu'à quatre rendez-vous.** Depuis l'arbitrage DET-35, une réponse juste fait progresser tout couple (compétence, cause) dont un distracteur de la question porte la cause. Une question à quatre distracteurs en porte jusqu'à quatre : le candidat qui l'a évitée les a effectivement tous évités, la progression est donc logiquement juste — mais elle devient rapide et bon marché. Deux garde-fous existent déjà et suffisent aujourd'hui : la certitude déclarée module le palier (`prochainPalier`), et la sortie du calendrier reste gardée par deux réussites CERTAINES consécutives. Non bridé volontairement — brider maintenant serait calibrer sans données. Point de réétalonnage : mesurer la vitesse réelle de vidage du calendrier avant d'ajouter une règle. | PAS-18, arbitrage DET-35 | Avec DET-19, sur données réelles |
| ~~DET-38~~ | ~~**La cause reste fermée dans la liste de révision même quand le candidat l'a déjà révélée.** `ReviewScheduleResource` ouvre le champ `cause` aux seuls titulaires de `CAUSE_REVEAL` et ne décompte jamais le quota F03 — décompter parce qu'un écran de liste s'ouvre brûlerait les deux révélations gratuites sans que le candidat ait rien demandé. Effet de bord : une cause déjà révélée sur une correction (`responses.cause_revealed`) réapparaît verrouillée dans la liste. Ce n'est pas une fuite, c'est une incohérence d'affichage au détriment du candidat. La corriger suppose de croiser la liste avec les révélations déjà faites, soit une lecture de plus par ligne — à faire avec l'écran qui l'exposera vraiment.~~ **Clos au PAS-19 : ce n'était pas un prix à assumer, c'était une contradiction.** `ParcoursController::correction()` engage le produit — « le quota est décompté une seule fois par réponse, revenir sur sa correction ne recoûte rien » — et `CauseRevealCounter` n'est jamais remis à zéro pour cette raison même. Retrouver fermée trois jours plus tard une cause déjà payée n'est pas un mur payant, c'est une promesse rompue. `CauseRevealService::revealedCouples()` rend en UNE requête l'ensemble des couples (compétence, cause) déjà révélés — l'objection du coût par ligne, qui avait fait renoncer, ne tenait pas. Le mur reste entier sur ce qui n'a jamais été révélé, et aucune unité ne se consomme à la lecture d'une liste. | PAS-18, `ReviewScheduleResource` | clos · PAS-19 |
| DET-49 | **La page de couverture lit tous les rendez-vous de l'épreuve, sans borne et à chaque affichage.** `CouvertureBanque::demande()` regroupe `review_schedules` par couple sur l'épreuve entière : le nombre de LIGNES RENDUES est borné par le catalogue — les nœuds de compétence croisés avec les huit causes — mais le nombre de lignes PARCOURUES croît avec les candidats. La page n'est ni paginée ni mise en cache, volontairement : paginer une liste ordonnée par urgence cacherait justement ce qu'on veut voir en premier, et mettre en cache un plan de rédaction le ferait mentir le jour où il compte. Sans effet aujourd'hui — la base de test compte quelques dizaines de rendez-vous — mais c'est la première requête du back-office dont le coût suit l'usage réel du produit, et elle est sur l'écran d'accueil, donc jouée à chaque connexion. À mesurer avant la première cohorte, pas après. | Lot A4, constaté | À la première cohorte réelle |
| DET-50 | **`QuestionTransitionService::retire()` accepte un `?string $reason` et ne l'écrit NULLE PART.** Le paramètre existe depuis le PAS-5, aucune colonne ne le reçoit, aucun appelant ne le fournit. Il donne l'apparence d'une traçabilité qui n'existe pas : un lecteur du code croit que le motif de retrait est conservé. **Constaté en inventoriant les transitions pour le PAS-33, signalé et non corrigé** — la consigne demandait d'exposer ce qui existe, pas d'ajouter une règle en passant. Deux issues : supprimer le paramètre, ou lui donner une colonne et une route. La seconde suppose de trancher qui doit motiver un retrait et si le motif est lisible par un candidat — décision de produit, pas de code. **Et le refus motivé n'existe pas non plus** : `a_verifier → draft`, `reviewed → a_verifier` et `pedagogically_validated → reviewed` sont des arêtes DÉCLARÉES dans la table des transitions, mais `transition()` est privée et aucune méthode publique ne les emprunte. Renvoyer une question à son rédacteur est donc impossible par quelque chemin que ce soit, back-office compris. | PAS-33, constaté | Avant la première équipe éditoriale à plus de deux personnes |
| DET-51 | **`RATE_LIMIT_PROFILE` n'est documenté que dans `config/naja7i.php`, pas dans `.env.example`.** La variable existe depuis le PAS-34 et pilote tous les seuils de transport ; un exploitant qui lit le seul `.env.example` ne la découvrira pas, et un poste de recette continuera d'attendre les fenêtres sans savoir qu'il n'a pas à le faire. Le fichier n'a pas été touché au PAS-34 : l'environnement de travail de la session interdit l'écriture dans les fichiers en `.env*`, et contourner l'interdiction pour une ligne de documentation aurait été le mauvais arbitrage. Une ligne à ajouter, avec son commentaire — `# recette : relève les seuils de TRANSPORT, jamais la file d'envoi ni la sécurité`. | PAS-34, constaté | Au prochain pas touchant `.env.example` |
| DET-52 | **La longueur d'un examen blanc est une convention du produit, pas une donnée officielle.** `blueprints.official_question_count` est NUL pour les trois épreuves, et `official_scoring_note_fr` dit explicitement que le barème n'est pas précisé par le descriptif. Le simulateur a pourtant besoin d'un nombre : il vient de `naja7i.simulation.default_question_count` (20 par défaut, `SIMULATION_QUESTION_COUNT`). Le rapport publie l'absence de barème à côté du score, donc le candidat n'est pas trompé — mais la LONGUEUR reste une décision d'ingénierie affichée comme un examen blanc. Le jour où un descriptif donne le nombre réel, cette clé doit devenir une lecture de `official_question_count`, et le simulateur cesse d'avoir une convention à assumer. À ne pas confondre avec la RÉPARTITION, qui suit les poids officiels et n'est pas en dette. | PAS-35, constaté à l'inventaire | À la première publication d'un descriptif donnant le format |
| DET-53 | **Un examen blanc rejoué peut resservir les mêmes questions.** `DiagnosticComposer` ne borne pas la sélection à ce que le candidat n'a jamais vu — c'est sans conséquence pour un diagnostic, qui se passe une fois, mais un candidat qui enchaîne trois examens blancs sur une banque de vingt questions les reverra. Non corrigé au PAS-35, et délibérément : exclure le déjà-vu ferait dériver la répartition dès que la banque se raréfie sur un domaine, ce qui casserait précisément la propriété qui autorise la note (la série reproduit les poids). Le vrai remède est une banque plus fournie, pas un filtre. À rouvrir si un candidat réel enchaîne les simulations, avec la mesure sous les yeux. | PAS-35, signalé | À la première cohorte réelle, avec DET-49 |
| ~~DET-54~~ | ~~**Les deux citations officielles du descriptif n'existent qu'en français.** `blueprints.official_scoring_note_fr` et `official_admission_threshold_note_fr` n'ont AUCUNE colonne `_ar`, alors que `coverage_note` en a une. Le rapport d'examen blanc les sert telles quelles : un candidat arabophone lit « Barème détaillé non précisé par le descriptif officiel » en français, sur une page en arabe. `dir="auto"` rend le mélange lisible, ce qui masque le problème sans le régler.~~ **Clos au PAS-36 : les deux colonnes existent, et la ressource lit par `localized()`.** Nullables comme leurs jumelles françaises — ajouter la colonne n'oblige pas à la remplir, et la règle des blueprints ne change pas : ces champs restent vides tant qu'aucune source ne les établit. **Le repli reste le FRANÇAIS et non le vide** : une citation vraie mais pas encore traduite vaut mieux que rien, et c'est ce que le produit faisait déjà — à ceci près qu'il n'avait alors aucune autre issue. Deux tests, dont un sur le repli ; la mutation qui remet le champ `_fr` en dur fait rougir la localisation. | PAS-35, constaté à la recette FRONT-6 | clos · PAS-36 |
| DET-55 | **La TVA n'est ni calculée ni affichée, et le prix n'est ni HT ni TTC — il est juste « le prix ».** `plans.price_cents` porte un montant nu. Tant que l'encaissement passe par un virement et un coupon validé à la main, l'obligation fiscale se traite hors du produit ; dès qu'un prestataire encaisse, elle ne le peut plus. **La décision qui manque n'est pas technique** : afficher TTC (usage marocain grand public) ou HT, et sous quel régime — elle appartient à OptimGov, et le modèle la suivra en une migration. Ce qui est en dette ici est donc l'ABSENCE DE DÉCISION, pas l'absence de code. | Lot ABO, signalé | Avant le premier encaissement par prestataire |
| DET-56 | **Aucun remboursement, aucun avoir.** `refunds.issue` existe au référentiel de permissions depuis le PAS-9 et n'a aucune surface. Un octroi posé ne se révoque pas depuis les commandes : il faudrait le raccourcir ou le supprimer, et décider ce que devient un droit partiellement consommé. Sans prestataire, le cas se traite à la main et reste rare ; avec un prestataire, il devient un droit du consommateur assorti d'un délai légal. À ouvrir EN MÊME TEMPS que le vrai adaptateur, jamais après. | Lot ABO, signalé | Avec le premier prestataire de paiement |
| DET-57 | **Aucune facture, aucun justificatif téléchargeable.** Un candidat qui paie 199 MAD n'a rien à présenter à son employeur ou à son comptable, et une AREF qui achète cinquante coupons n'a que sa note interne. Le modèle porte pourtant tout ce qu'il faut — montant figé, devise, date d'honoration, plan, validateur. Ce qui manque est le document et son numérotage, qui est une contrainte réglementaire avant d'être une fonctionnalité. | Lot ABO, signalé | Avec DET-55, même décision fiscale |
| DET-58 | **Le troisième adaptateur de paiement n'existe pas, et l'interface n'a donc jamais été éprouvée par un TROISIÈME cas.** `PaymentGateway` a deux implémentations, qui se ressemblent : toutes deux ouvrent une commande en une passe, sans aller-retour. Un vrai prestataire ajoute une notification serveur à serveur, différée, rejouable et signée — c'est ce qui dira si l'interface tient. Deux implémentations ne prouvent pas une abstraction ; elles la rendent seulement plausible. Signalé pour que la découverte ne passe pas pour une surprise. | Lot ABO, constaté | Avec le premier prestataire |
| DET-59 | **`status` d'un coupon peut diverger des faits.** `Coupon::estUtilisable()` tranche sur les DATES et le COMPTEUR ; `status` sert à la lecture humaine en back-office. Un coupon expiré par sa date reste donc affiché `actif` jusqu'à ce que quelqu'un le regarde. C'est délibéré — deux sources dont une seule décide vaut mieux qu'un état à maintenir en cohérence par des tâches planifiées — mais un exploitant qui filtre sur `status` obtiendra une liste optimiste. À corriger par une colonne calculée ou une vue, pas par un cron. | Lot ABO, constaté | Quand la volumétrie de coupons rendra la liste illisible |
| DET-60 | **Les poids qui composent les diagnostics viennent d'une source identifiée mais INVÉRIFIABLE.** `SE-PSY` 40 / `SE-PED` 30 / `SE-SOC` 30 portent `source_id → SRC-CRMEF-2025-SE`, dont `verified_at` est NUL. La chaîne remonte à `docs/regles/CRMEF-2025-referentiel-source.md` §7.2, qui déclare transcrire un descriptif officiel — et dont la ligne 23 dit : « Tu ne recevras pas les PDF officiels […] Ne prétends toutefois pas posséder ou avoir consulté les PDF. » Le corpus du 15 août confirme qu'aucun cadre de référence n'est dans les 33 fichiers. Ce ne sont donc pas des poids inventés, ce sont des poids RAPPORTÉS dont personne ici n'a vu la pièce d'origine — et `DiagnosticComposer` s'y règle. Ne pas corriger seul : obtenir le descriptif, ou décider d'assumer des poids `observed` établis en classant les 1 411 annales. | CRMEF-2, phase 1 | **Décision du pilote** |
| DET-61 | **La voie C — secondaire 2e grade, session 2023 — n'est pas modélisée**, alors que 14 des 25 sujets du corpus en relèvent et qu'elle porte le seul blueprint intra-bloc complet du corpus (physique-chimie, §1.5.1). Elle ne peut pas être importée sous la voie B : intitulé imprimé, autorité émettrice, nombre d'options et barème diffèrent tous. | CRMEF-2, phase 1 | Avant l'import des annales 2023 |
| DET-62 | **Le coefficient 12 (didactique) et le coefficient 20 (spécialité) de la voie française 2025 ne sont corroborés par aucun document du dépôt.** Le corpus imprime 12 pour la didactique française de 2023 (voie C) et 20 pour la spécialité PHYSIQUE-CHIMIE 2023 — jamais pour le français 2025. Les trois durées de la base (120, 120, 240) coïncident elles aussi exactement avec les durées imprimées en 2023. Constat, sans conclusion : le corpus ne contient aucun descriptif 2025 et ne peut donc ni confirmer ni réfuter. | CRMEF-2, phase 1 | Avec DET-60 |
| DET-63 | **`ADR-0014` §3 s'intitule « Les poids officiels sont vérifiables » et décrit une vérification ARITHMÉTIQUE.** `ReferentielCrmefTest` prouve que les enfants somment au parent et les racines à 100 ; il ne prouve rien sur la provenance. Un jeu de poids entièrement faux mais cohérent passerait. Le titre laisse croire l'inverse et doit être amendé. | CRMEF-2, phase 1 | Prochain passage sur l'ADR |
| DET-64 | **Contradiction imprimée sur le coefficient de علوم التربية 2023** : `المعامل : 5` en version arabe, `المعامل 8` en version française, dans le même fichier de sujet. Le corpus refuse de trancher (§1.5.3) et exige une source officielle avant tout calcul de score. À noter que 8 est la valeur imprimée en 2024 et 2025 dans les trois voies. | CRMEF-2, phase 1 | Avant tout usage de la voie C dans un score |
| DET-65 | **Réserve de droits sur les 25 sujets.** Ce sont des reproductions diffusées par un tiers sous filigrane `www.educaprof.com`, dont plusieurs portent un filigrane nominatif de personne physique. Le sujet émane d'une autorité publique, mais la republication commerciale d'une reproduction est une question distincte, non tranchée. S'ajoute une réserve d'intégrité : 10 des 25 sont démontrablement amputés. | Corpus §5.4 | **Avant toute mise en ligne** |
| DET-66 | **DETTE ÉDITORIALE : notre banque entraîne à un format d'épreuve qui n'existe plus depuis 2024.** Les sujets 2024 et 2025 impriment CINQ options (« اقتُرحت لكل سؤال خمسة أجوبة (A ; B ; C ; D ; E) ») ; nos questions en comptent quatre. Chiffré au 16 août : **21 questions de démonstration, toutes sur `CRMEF-FR-SPEC-2025` — 20 publiées, 1 validée pédagogiquement, toutes à 4 options.** Elles restent conformes AUJOURD'HUI parce que cette épreuve ne déclare pas son nombre : aucun sujet français 2025 n'est dans le corpus. Le jour où un descriptif l'établit à 5 — ce que la régularité §1.6 rend probable — ces 21 questions deviennent non publiables. Le remède n'est pas du code : chaque question doit gagner une cinquième option avec sa justification et sa cause. Le corpus dit ce qu'elle est presque toujours : « جميع الاختيارات المقترحة خاطئة » / « Aucune des propositions précédentes ». | CRMEF-2, phase 2 | Avant de déclarer `options_count` sur les épreuves françaises |
| DET-67 | **L'option E n'a pas de code de cause qui lui convienne.** Son distracteur est un piège de MÉTHODE — « l'élimination progressive cesse de fonctionner » (§4.2.1) — et aucun des huit codes de `error_cause` ne le porte : ni confusion de notions, ni lecture d'énoncé, ni connaissance absente. `indetermine` a été employé dans les fixtures de la phase 2, faute de mieux et sans rien inventer. Voir `docs/regles/CRMEF-causes-observees.md` §3 et §4 : deux ou trois codes seraient appelés par les faits imprimés, mais leur ajout appartient à DET-16, non tranché. | CRMEF-2, phase 2 | Avec DET-16 |
| DET-68 | **La chaîne CI/CD des branches `cicd-2` (backend) et `cicd` (frontend) est produite HORS DÉPÔT par une session tierce, sans consigne inscrite au dépôt.** Attribution établie le 16 août : c'est la session qui prépare le déploiement, travaillant sur le disque à travers le pont du bureau dans un environnement calé sur UTC — d'où le reflog en +0000, qui est la signature du PONT et non d'un tiers inconnu. Un agent, trois dépôts, trois minutes : backend 04:41:18, frontend 04:41:42, et `Naja7i_infra/docs/PREPROD_OPTIMGOV.md` à 03:44:02 UTC. Les deux anomalies relevées à l'analyse s'expliquent par là : `deploiement.yml` diffère de 156 lignes de la branche `cicd` parce qu'il a été RÉÉCRIT pour l'exécuteur auto-hébergé, et l'action composite `.github/actions/deployer/action.yml` a été retirée parce qu'elle portait la connexion SSH, devenue sans objet — retirée plutôt que laissée à faire croire qu'elle sert. **CE QUI RESTE, ET NE CHANGE PAS : les deux branches se relisent LIGNE À LIGNE avant tout usage, les deux `deploiement.yml` en premier.** Non plus parce que leur origine serait douteuse — elle ne l'est plus — mais parce que ce sont les fichiers qui s'exécutent sur le serveur avec les secrets. La matière est bonne, et c'est précisément ce qui la rend tentante à adopter sans la relire : `server/routes/sante.ts` porte un raisonnement juste sur la sonde de vivacité, ce qui n'est pas une raison de le prendre sans l'éprouver. Les deux branches restent locales et non poussées ; `origin/cicd` (backend, `51730bd`) reste sur le distant, et la relecture dira laquelle des deux fait foi. | Session tierce de déploiement, constat du 16 août | **Ouverture du lot de déploiement** |
| DET-69 | **160 questions classées restent dehors, faute d'épreuve où les rattacher.** Sur les 213 du classement, seules les 53 du bloc `2025_SCED_college_qualifiant` pendent d'une épreuve modélisée : mesuré, les neuf nœuds `SE-*` appartiennent tous à `CRMEF-SE-2025`, voie B. Les 160 autres viennent des voies A (primaire : 21 + 20 + 21) et C (secondaire 2e grade : 54 + 44), que le dépôt ne modélise pas — décision du pilote, DET-61. Elles ne s'inventent pas de nœud et ne s'importent pas sous une épreuve qui n'est pas la leur : autre intitulé, autre autorité émettrice, autre barème, autre nombre d'options. `--tous` est refusé par la commande, avec ce motif. Elles entreront le jour où les voies A et C auront leur arbre, ce qui suppose leurs descriptifs. | CRMEF-2, phase 3 | Avec DET-61 |
| DET-70 | **1 166 questions du corpus n'ont aucun arbre de compétences où entrer.** Ce sont les didactiques et les savoirs de spécialité : le classement les laisse sans code, avec pour motif « relève d'un autre arbre de compétences que l'arbre SE fourni ; arbre non communiqué ». Ce n'est pas un défaut du classement mais l'état du référentiel : nous n'avons de taxonomie que pour علوم التربية. S'y ajoutent 32 questions de علوم التربية non attribuées, chacune avec un motif propre — docimologie, besoins éducatifs particuliers, gouvernance — qui sortent des six sous-domaines. | CRMEF-2, phase 3 | Après obtention des descriptifs de didactique et de spécialité |
| DET-71 | **`QuestionMiroirTest > un couple sans miroir apparaît au plan de rédaction` est instable, et l'était sans être inscrit nulle part.** Échec en CI le 13 août (exécution 31699910560) : `assertSame('no_sibling', $trou['coverage']['fr']['severity'])` a reçu `'none'`. Rien ne le traçait — ni ce fichier, ni le journal, ni les workflows : une anomalie connue portée trois jours, exactement le motif de la règle permanente 9. **CE QUE JE SAIS** : la forme exacte de l'échec ; qu'il passe isolément ; que `severity` se calcule sur l'état de la banque, donc qu'il dépend de ce que les autres tests y ont laissé ; qu'un échec de même famille — `RendezVousMemoireTest > le plan de rédaction…` — s'est produit une fois dans cette session sur une exécution complète, puis n'a pas reparu en deux passages. **CE QUE J'IGNORE** : s'il s'agit d'une dépendance à l'ordre, d'un fuseau, ou d'un partage de compteur ; et si les deux classes échouent pour la même raison. **POURQUOI C'EST URGENT MAINTENANT** : l'exécuteur auto-hébergé déploie depuis `main`, donc un rouge au hasard bloquera un déploiement sans rapport avec son contenu. Se ferme quand le test est rendu déterministe OU expliqué — et dans les deux cas, avec le genre d'instabilité qu'il illustre. | CI du 13 août, inscrite le 16 avant recherche | **Avant le premier déploiement automatisé** |
